import { Box, Typography } from '@mui/material';

const Footer = () => {
  return (
    <Box variant="footer">
      <Typography>Lorem ipsum this is footer</Typography>
    </Box>
  );
};

export default Footer;
