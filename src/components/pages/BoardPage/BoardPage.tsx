import { Typography } from '@mui/material';
import Wrapper from '@/components/atoms/Wrapper/Wrapper.tsx';

const BoardPage = () => {
  return (
    <Wrapper>
      <Typography component="h2">Board Page</Typography>
    </Wrapper>
  );
};

export default BoardPage;
