import { theme } from '@/lib/theme.tsx';

export const unauthorizedIconWrapperStyles = {
  position: 'relative',
  backgroundColor: theme.palette.primary.main,
};

export const unauthorizedIconStyles = {
  zIndex: 5,
  maxWidth: '350px',
  maxHeight: '350px',
  borderRadius: '0.5rem',
};
